@using FinanceTracker.Contracts.Classifications
@using FinanceTracker.Extensions
@using FluentValidation
@inject IValidator<AddCustomClassificationsToTransaction> Validator
<DialogBase TResponse="AddCustomClassificationsToTransaction" Title="Add Classification / Tag To Transaction"
            ResponseConstructor="BuildResponseModel">
    <Content>
        <MudForm @ref="_form" Model="@_model">
            <MudDataGrid T="Classification" MultiSelection="true" Items="@Classifications"
                         SortMode="SortMode.Multiple" Filterable="true" QuickFilter="@QuickFilter"
                         Hideable="false"
                         SelectedItemsChanged="@SelectedItemsChanged">
                <ToolBarContent>
                    <MudText Typo="Typo.h6">Classifications / Tags</MudText>
                    <MudSpacer/>
                    <MudTextField @bind-Value="_searchString" Placeholder="Search classifications / tags"
                                  Adornment="Adornment.Start" Immediate="true"
                                  AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium"
                                  Class="mt-0"></MudTextField>
                </ToolBarContent>
                <Columns>
                    <SelectColumn T="Classification"/>
                    <PropertyColumn Property="x => x.Tag" Title="Classification / Tag" Sortable="true" Filterable="true"/>
                </Columns>
                <PagerContent>
                    <MudDataGridPager T="Classification"/>
                </PagerContent>
            </MudDataGrid>
            <MudTextField T="IEnumerable<SelectedCustomClassification>"
                          @bind-Value="_model.Classifications"
                          For="@(() => _model.Classifications)"
                          Validation="@(Validator.ValidateValue(_model, nameof(AddCustomClassificationsToTransaction.Classifications)))"
                          Style="display: none;"/>
        </MudForm>
    </Content>
</DialogBase>

@code {

    private MudForm _form = null!;
    private AddCustomClassificationsToTransaction _model = null!;

    [Parameter] public Guid TransactionId { get; set; }

    [Parameter] public IEnumerable<Classification> Classifications { get; set; } = null!;

    private string _searchString = string.Empty;

    protected override void OnInitialized()
    {
        _model = new AddCustomClassificationsToTransaction
        {
            TransactionId = TransactionId,
            Classifications = []
        };
    }

    private Func<Classification, bool> QuickFilter => x =>
    {
        if (string.IsNullOrWhiteSpace(_searchString))
            return true;

        return x.Tag != null && x.Tag.Contains(_searchString, StringComparison.OrdinalIgnoreCase);
    };


    private async Task<AddCustomClassificationsToTransaction?> BuildResponseModel()
    {
        await _form.Validate();
        if (!_form.IsValid) return null;

        return _model;
    }

    void SelectedItemsChanged(HashSet<Classification> items)
    {
        _model.Classifications = items.Select(x => new SelectedCustomClassification { ClassificationId = x.ClassificationId }).ToList();
        _form.Validate();
    }

}
